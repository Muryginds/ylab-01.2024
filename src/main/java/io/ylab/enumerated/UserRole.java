package io.ylab.enumerated;

/**
 * Enumeration representing user roles in the system.
 *
 * <p>This enum defines roles, which represent the roles that users can have within the system.
 */
public enum UserRole {
    USER,
    ADMIN
}
